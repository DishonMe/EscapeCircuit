# Riddle 04: Palindrome Detector

This riddle requires implementing a logic circuit that returns true if the 4 input bits act as a palindrome.
The user must understand equality checking using logic gates (XNOR or XOR+NOT).
